apiVersion: v1
kind: Service
metadata:
  name: api-service                        # → ingress.service.name
  labels:
    app: api
spec:
  selector:
    app: api                       # → deployment.labels
  ports:
  - port: 80                      # → ingress.port
    targetPort: 3000                # → deployment.containerPort

